@* @model DummyProject.Models.ViewModel.HybridViewModel

@{
    ViewData["Title"] = "Order Confirmation";
}

<link rel="stylesheet" href="~/css/checkout.css" asp-append-version="true" />

<main class="checkout-container">
    <div class="checkout-header text-center">
        <h1>Thank You for Your Order! <i class="fas fa-check-circle text-success"></i></h1>
        <p>Your order has been placed successfully.</p>
    </div>
</main>

<div class="container checkout-content">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card bg-dark text-white p-4" style="border-radius: 12px; border: 1px solid var(--border);">
                <h3 class="mb-3"><i class="fas fa-receipt"></i> Order Summary</h3>
                <ul class="order-items list-unstyled">
                    @if (Model?.CartProducts != null && Model.CartProducts.Any())
                    {
                        @foreach (var cartProduct in Model.CartProducts)
                        {
                            <li class="d-flex justify-content-between">
                                <span>@cartProduct?.Product?.ProductTitle</span>
                                <span>$@(cartProduct?.Product?.Price * cartProduct?.Quantity ?? 0)</span>
                            </li>
                        }
                    }
                </ul>
                <hr>
                <div class="totals-row grand-total d-flex justify-content-between">
                    <span><strong>Total Paid</strong></span>
                    <strong>$@(Model?.Cart?.CartValue ?? 0)</strong>
                </div>
                <div class="mt-4">
                    <h5>Shipping To:</h5>
                    <div>
                        @Model?.User?.FullName <br />
                        @Model?.Address?.AddressLine <br />
                        @Model?.Address?.City, @Model?.Address?.State <br />
                        @Model?.Address?.ZipCode, @Model?.Address?.Country
                    </div>
                </div>
                <div class="mt-4 text-center">
                    <a href="~/" class="btn btn-primary">Continue Shopping</a>
                </div>
            </div>
        </div>
    </div>
</div> *@